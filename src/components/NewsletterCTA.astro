---
---

<section class="relative py-16 overflow-hidden">
  <!-- Gradient Background -->
  <div class="absolute inset-0 bg-gradient-to-br from-primary via-secondary to-accent opacity-90"></div>

  <!-- Subtle pattern overlay -->
  <div class="absolute inset-0 opacity-10">
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_1px_1px,currentColor_1px,transparent_0)] bg-[length:24px_24px]"></div>
  </div>

  <div class="relative container mx-auto px-4 text-center">
    <div class="max-w-2xl mx-auto">
      <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">
        Tetap Update dengan Berita Tech Terbaru
      </h2>
      <p class="text-lg text-white/90 mb-8 leading-relaxed">
        Dapatkan berita harian tentang AI, cloud computing, cybersecurity, dan teknologi terbaru langsung ke inbox Anda.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 max-w-md mx-auto">
        <input
          type="email"
          placeholder="Masukkan email Anda..."
          class="flex-1 px-4 py-3 rounded-lg border-0 focus:ring-2 focus:ring-white/50 focus:outline-none text-gray-900"
        />
        <button class="btn-primary bg-white text-primary hover:bg-white/90 font-semibold shadow-lg">
          Berlangganan
        </button>
      </div>

      <p class="text-sm text-white/70 mt-4">
        ✨ Gratis! Tidak ada spam, unsubscribe kapan saja.
      </p>
    </div>
  </div>
</section>

<style>
  /* Enhanced gradient animation */
  @keyframes gradient-shift {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  .bg-gradient-to-br {
    background-size: 200% 200%;
    animation: gradient-shift 8s ease infinite;
  }

  /* Dark mode adjustments */
  :global(.dark) .bg-gradient-to-br {
    opacity: 0.95;
  }
</style>